# Dependencies
node_modules/

# Build output
dist/

# Secrets — never commit your real Supabase keys
.env
.env.local

# Native platform projects (generated by `npx cap add` — don't need to be
# in git; Xcode/Android Studio regenerate them from web + capacitor.config)
ios/
android/

# OS/editor junk
.DS_Store
*.log
